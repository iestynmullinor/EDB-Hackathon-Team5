[project]
name = "datagen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "faker>=40.13.0",
    "google-cloud-firestore>=2.26.0",
    "pandas>=3.0.2",
]
